class CartItem {
    constructor(id, quantity, price, title, sum, image) {
        this.id = id;
        this.quantity = quantity;
        this.price = price;
        this.title = title;
        this.sum = sum;
        this.image = image;
    }
}

export default CartItem;
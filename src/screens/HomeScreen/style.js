import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    screen: {
        flexGrow: 1,
        marginTop: 8,
    },
    title: {
        paddingBottom: 5,
        fontSize: 32,
        fontWeight: "bold",
        color: "white"
    },
    slider: {
        borderRadius: 20,
    }
})

export default styles;
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    screen: {
        flex: 1
    },
    welcomTextContainer: {
        marginTop: 10,
        alignItems: "center"
    },
    welcomText: {
        fontSize: 22,
        fontWeight: "bold",
        color: "#343434",
    },  
    bgImage: {

    }
})

export default styles;